// 라이브러리
import { css } from "styled-components";

const CalculateResultStyle = css`
  width: 50%;
  background: ${(props) => props.theme.color.deepDark};
  border-radius: 4px;
`;

export default CalculateResultStyle;
